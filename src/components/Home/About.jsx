import { Typography } from '@mui/material';

const About = () => {
  return (
    <div>
      <Typography>技術</Typography>
      <Typography>使用</Typography>
      <Typography>構想</Typography>
    </div>
  );
};

export default About;
